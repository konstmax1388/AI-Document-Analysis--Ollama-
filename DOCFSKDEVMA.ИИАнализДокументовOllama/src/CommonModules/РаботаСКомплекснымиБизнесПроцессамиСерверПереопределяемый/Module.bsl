
#Область ПрограммныйИнтерфейс

// Точка входа при вызове из кода (не из строки Выполнить), либо если платформа отрабатывает переопределяемый модуль.
// Скрипт схемы обработки объекта (поле алгоритма), при наличии lvv_ОтклБезопасныйРежимВыполненияСкриптов:
// ИАДО_СценарийСхемыОбработкиОбъектаВызовСервера.ИАДО_ЗапуститьАнализИИИзСкриптаСхемыОбработкиОбъекта(Параметры);
//
// Параметры:
//  ДокументСсылка — СправочникСсылка.ДокументыПредприятия — обычно Параметры.ОбработкаОбъект.Владелец.
//
Процедура ИАДО_ЗапуститьАнализФайлаИзСкриптаСхемыПоДокументу(Знач ДокументСсылка) Экспорт

	ИАДО_ИнтеграцияИИВызовСервера.ЗапуститьАнализФайлаИзСкриптаСхемыПоДокументу(ДокументСсылка);

КонецПроцедуры

// Синоним для скрипта схемы: имя начинается с глагола — проще не перепутать с ОбработкаОбъект.ИАДО_...
//
// Параметры:
//  ДокументСсылка — СправочникСсылка.ДокументыПредприятия — Параметры.ОбработкаОбъект.Владелец.
//
Процедура ЗапуститьАнализИИПоДокументуИзСкриптаСхемыОбработки(Знач ДокументСсылка) Экспорт

	ИАДО_ЗапуститьАнализФайлаИзСкриптаСхемыПоДокументу(ДокументСсылка);

КонецПроцедуры

#КонецОбласти
