
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	ЗагрузитьКонстантыВРеквизиты();
	РедактированиеДоступно = ПравоДоступа("Update", Метаданные.Константы.ИАДО_OllamaБазовыйURL);
	НастроитьДоступностьЭлементов();

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Записать(Команда)

	Если Не РедактированиеДоступно Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(
			НСтр("ru = 'Нет прав на изменение констант расширения.'"));
		Возврат;
	КонецЕсли;

	Результат = ЗаписатьКонстантыНаСервере();
	Если Не Результат.Успех Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(Результат.СообщениеОбОшибке);
		Возврат;
	КонецЕсли;

	ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Настройки записаны.'"));

КонецПроцедуры

&НаКлиенте
Процедура ТестOllama(Команда)

	Если Не РедактированиеДоступно Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(
			НСтр("ru = 'Для теста нужны права на запись констант: сначала сохраните настройки в базе.'"));
		Возврат;
	КонецЕсли;

	РезультатЗаписи = ЗаписатьКонстантыНаСервере();
	Если Не РезультатЗаписи.Успех Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(РезультатЗаписи.СообщениеОбОшибке);
		Возврат;
	КонецЕсли;

	РезультатТеста = ИАДО_НастройкиАнализаИИВызовСервера.ТестПодключенияСПромптомПоКонстантам(Истина);
	ПоказатьРезультатТеста(
		РезультатТеста,
		НСтр("ru = 'Подключение успешно. Ответ модели:'"),
		НСтр("ru = 'Ошибка подключения к Ollama'"));

КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьМоделиOllama(Команда)

	Если Не РедактированиеДоступно Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(
			НСтр("ru = 'Нужны права на запись констант: команда использует URL из сохранённых настроек.'"));
		Возврат;
	КонецЕсли;

	РезультатЗаписи = ЗаписатьКонстантыНаСервере();
	Если Не РезультатЗаписи.Успех Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(РезультатЗаписи.СообщениеОбОшибке);
		Возврат;
	КонецЕсли;

	Результат = ИАДО_НастройкиАнализаИИВызовСервера.ОбновитьКэшМоделейOllamaПоКонстантам();
	ТекстСообщения = Результат.ТекстДляПользователя;
	Если ПустаяСтрока(ТекстСообщения) Тогда
		ТекстСообщения = ?(Результат.Успех, НСтр("ru = 'Список моделей обновлён.'"),
			НСтр("ru = 'Не удалось получить список моделей с сервера Ollama.'"));
	КонецЕсли;
	ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);

КонецПроцедуры

&НаКлиенте
Процедура ВключитьВсеМоделиOllama(Команда)

	Если Не РедактированиеДоступно Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(
			НСтр("ru = 'Нужны права на запись констант.'"));
		Возврат;
	КонецЕсли;

	РезультатЗаписи = ЗаписатьКонстантыНаСервере();
	Если Не РезультатЗаписи.Успех Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(РезультатЗаписи.СообщениеОбОшибке);
		Возврат;
	КонецЕсли;

	Результат = ИАДО_НастройкиАнализаИИВызовСервера.ВключитьВсеМоделиOllamaССервераДляВыбораВЭксперте();
	ОбщегоНазначенияКлиент.СообщитьПользователю(Результат.ТекстДляПользователя);

КонецПроцедуры

&НаКлиенте
Процедура ТестОблакоИИ(Команда)

	Если Не РедактированиеДоступно Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(
			НСтр("ru = 'Для теста нужны права на запись констант.'"));
		Возврат;
	КонецЕсли;

	РезультатЗаписи = ЗаписатьКонстантыНаСервере();
	Если Не РезультатЗаписи.Успех Тогда
		ОбщегоНазначенияКлиент.СообщитьПользователю(РезультатЗаписи.СообщениеОбОшибке);
		Возврат;
	КонецЕсли;

	РезультатТеста = ИАДО_НастройкиАнализаИИВызовСервера.ТестПодключенияОблакоИИПоКонстантам(Истина);
	ПоказатьРезультатТеста(
		РезультатТеста,
		НСтр("ru = 'Подключение к облачному API успешно. Ответ модели:'"),
		НСтр("ru = 'Ошибка облачного API'"));

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьЭкспертыИИ(Команда)

	ОткрытьФорму("Справочник.ИАДО_ЭкспертыИИ.ФормаСписка");

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьПравилаЭкспертов(Команда)

	ОткрытьФорму("РегистрСведений.ИАДО_ПравилаЭкспертовДокументов.ФормаСписка");

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьЖурналИИ(Команда)

	ОткрытьФорму("РегистрСведений.ИАДО_ЖурналОбращенийКИИ.ФормаСписка");

КонецПроцедуры

&НаКлиенте
Процедура ОткрытьМоделиOllama(Команда)

	ОткрытьФорму("РегистрСведений.ИАДО_МоделиЛокальногоOllama.ФормаСписка");

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ЗагрузитьКонстантыВРеквизиты()

	OllamaБазовыйURL = Константы.ИАДО_OllamaБазовыйURL.Получить();
	OllamaТаймаутСек = Константы.ИАДО_OllamaТаймаутСек.Получить();
	OllamaМодельПоУмолчанию = Константы.ИАДО_OllamaМодельПоУмолчанию.Получить();
	ОблакоБазовыйURL = Константы.ИАДО_ОблакоИИБазовыйURL.Получить();
	Если ПравоДоступа("Read", Метаданные.Константы.ИАДО_ОблакоИИКлючAPI) Тогда
		ОблакоКлючAPI = Константы.ИАДО_ОблакоИИКлючAPI.Получить();
	Иначе
		ОблакоКлючAPI = "";
	КонецЕсли;
	ОблакоМодельПоУмолчанию = Константы.ИАДО_ОблакоИИМодельПоУмолчанию.Получить();
	ОблакоТаймаутСек = Константы.ИАДО_ОблакоИИТаймаутСек.Получить();
	ОблакоPDFТолькоТекст = Константы.ИАДО_ОблакоИИPDFТолькоТекст.Получить();

КонецПроцедуры

&НаСервере
Процедура НастроитьДоступностьЭлементов()

	ТолькоПросмотрПолей = Не РедактированиеДоступно;

	Элементы.OllamaБазовыйURL.ТолькоПросмотр = ТолькоПросмотрПолей;
	Элементы.OllamaТаймаутСек.ТолькоПросмотр = ТолькоПросмотрПолей;
	Элементы.OllamaМодельПоУмолчанию.ТолькоПросмотр = ТолькоПросмотрПолей;
	Элементы.ОблакоБазовыйURL.ТолькоПросмотр = ТолькоПросмотрПолей;
	Элементы.ОблакоКлючAPI.ТолькоПросмотр = ТолькоПросмотрПолей
		Или Не ПравоДоступа("Read", Метаданные.Константы.ИАДО_ОблакоИИКлючAPI);
	Элементы.ОблакоМодельПоУмолчанию.ТолькоПросмотр = ТолькоПросмотрПолей;
	Элементы.ОблакоТаймаутСек.ТолькоПросмотр = ТолькоПросмотрПолей;
	Элементы.ОблакоPDFТолькоТекст.ТолькоПросмотр = ТолькоПросмотрПолей;

	Элементы.ФормаЗаписать.Доступность = РедактированиеДоступно;
	Элементы.КомандаТестOllama.Доступность = РедактированиеДоступно;
	Элементы.КомандаЗагрузитьМодели.Доступность = РедактированиеДоступно;
	Элементы.КомандаВключитьВсеМодели.Доступность = РедактированиеДоступно;
	Элементы.КомандаТестОблако.Доступность = РедактированиеДоступно;

КонецПроцедуры

&НаСервере
Функция ЗаписатьКонстантыНаСервере()

	ПараметрыЗаписи = Новый Структура;
	ПараметрыЗаписи.Вставить("OllamaБазовыйURL", OllamaБазовыйURL);
	ПараметрыЗаписи.Вставить("OllamaТаймаутСек", OllamaТаймаутСек);
	ПараметрыЗаписи.Вставить("OllamaМодельПоУмолчанию", OllamaМодельПоУмолчанию);
	ПараметрыЗаписи.Вставить("ОблакоБазовыйURL", ОблакоБазовыйURL);
	ПараметрыЗаписи.Вставить("ОблакоКлючAPI", ОблакоКлючAPI);
	ПараметрыЗаписи.Вставить("ОблакоМодельПоУмолчанию", ОблакоМодельПоУмолчанию);
	ПараметрыЗаписи.Вставить("ОблакоТаймаутСек", ОблакоТаймаутСек);
	ПараметрыЗаписи.Вставить("ОблакоPDFТолькоТекст", ОблакоPDFТолькоТекст);

	Возврат ИАДО_НастройкиАнализаИИВызовСервера.ЗаписатьКонстантыИнтеграцииИИИзПараметровФормы(ПараметрыЗаписи);

КонецФункции

&НаКлиенте
Процедура ПоказатьРезультатТеста(РезультатТеста, ЗаголовокУспеха, ЗаголовокОшибки)

	Если РезультатТеста.Успех Тогда
		ТекстСообщения = СтрШаблон("%1%2%3", ЗаголовокУспеха, Символы.ПС, РезультатТеста.ТекстМодели);
	Иначе
		ТекстОшибки = РезультатТеста.ТекстДляПользователя;
		Если ПустаяСтрока(ТекстОшибки) Тогда
			ТекстОшибки = ЗаголовокОшибки;
		КонецЕсли;
		ТекстСообщения = ТекстОшибки;
	КонецЕсли;

	ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);

КонецПроцедуры

#КонецОбласти
