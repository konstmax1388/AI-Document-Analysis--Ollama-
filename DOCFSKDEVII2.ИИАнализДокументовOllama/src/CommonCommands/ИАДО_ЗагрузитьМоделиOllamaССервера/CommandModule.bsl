
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	Результат = ИАДО_НастройкиАнализаИИВызовСервера.ОбновитьКэшМоделейOllamaПоКонстантам();

	Если Результат.Успех Тогда
		ТекстСообщения = Результат.ТекстДляПользователя;
	Иначе
		ТекстСообщения = Результат.ТекстДляПользователя;
		Если ПустаяСтрока(ТекстСообщения) Тогда
			ТекстСообщения = НСтр("ru = 'Не удалось получить список моделей с сервера Ollama.'");
		КонецЕсли;
	КонецЕсли;

	ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);

КонецПроцедуры

#КонецОбласти
