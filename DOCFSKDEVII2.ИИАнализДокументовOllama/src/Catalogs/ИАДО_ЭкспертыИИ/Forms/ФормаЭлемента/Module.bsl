
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	НастроитьСписокВыбораИмениМоделиНаСервере();

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ПровайдерИИПриИзменении(Элемент)

	НастроитьСписокВыбораИмениМоделиНаСервере();

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура НастроитьСписокВыбораИмениМоделиНаСервере()

	Список = Элементы.ИмяМоделиOllama.СписокВыбора;
	Список.Очистить();

	Если Объект.ПровайдерИИ = Перечисления.ИАДО_ПровайдерыИИ.ОблакоOpenAIСовместимый Тогда

		ИменаОблака = ИАДО_НастройкиАнализаИИВызовСервера.ПодсказкиИменМоделейОблачногоИИ();
		Для Каждого Имя Из ИменаОблака Цикл
			Список.Добавить(Имя);
		КонецЦикла;

	Иначе

		Имена = ИАДО_НастройкиАнализаИИВызовСервера.СписокИменМоделейOllamaДляВыбораВЭксперте();
		Для Каждого Имя Из Имена Цикл
			Список.Добавить(Имя);
		КонецЦикла;

	КонецЕсли;

	Текущее = СокрЛП(Строка(Объект.ИмяМоделиOllama));
	Если Не ПустаяСтрока(Текущее) Тогда
		Найден = Ложь;
		Для Каждого ЭлементСписка Из Список Цикл
			Если ЭлементСписка.Значение = Текущее Тогда
				Найден = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		Если Не Найден Тогда
			Список.Добавить(Текущее);
		КонецЕсли;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти
