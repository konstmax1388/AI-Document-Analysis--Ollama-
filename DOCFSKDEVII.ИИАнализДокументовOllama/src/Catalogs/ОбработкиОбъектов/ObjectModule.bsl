#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ИАДО_СценарийСхемыОбработкиОбъекта

// Точка входа для скрипта схемы обработки объекта, если заимствование этого справочника в расширении
// совпало по UUID с вашей информационной базой.
// Предпочтительно вызывать менеджер Справочники.ДокументыПредприятия — см. ManagerModule расширения каталога «Документы предприятия».
// Текст элемента схемы (запасной вариант):
//   Параметры.ОбработкаОбъект.ИАДО_ЗапуститьАнализИИИзСкриптаСхемыОбработкиОбъекта();
//
// Не вызывать из скрипта ОбщегоНазначения.ВыполнитьМетодКонфигурации: внутри используется Выполнить,
// что недопустимо при уже включённом безопасном режиме в РаботаСПроцессамиПоОбработкамОбъектов.ВыполнитьСкриптСхемы.
//
Процедура ИАДО_ЗапуститьАнализИИИзСкриптаСхемыОбработкиОбъекта() Экспорт

	ИАДО_ИнтеграцияИИВызовСервера.ЗапуститьАнализФайлаИзСкриптаСхемыПоДокументу(Владелец);

КонецПроцедуры

#КонецОбласти

#КонецЕсли
